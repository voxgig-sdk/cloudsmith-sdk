# BasicMavenFlow

main: kit: flow: BasicMavenFlow:
{
  entity: "maven",
  kind: "basic",
  name: "BasicMavenFlow",
  step: [
    {
      input: {
        ref: "maven_ref01"
      }

      match: {
        identifier: "identifier01",
        owner: "owner01"
      }

      op: "create"
    }

    {
      match: {
        identifier: "identifier01",
        owner: "owner01"
      }

      op: "list",
      valid: [
        {
          apply: "ItemExists",
          def: {
            ref: "maven_ref01"
          }
        }
      ]
    }

    {
      data: {
        identifier: "identifier01",
        owner: "owner01"
      }

      input: {
        ref: "maven_ref01",
        srcdatavar: "maven_ref01_data",
        suffix: "_up0",
        textfield: "auth_mode"
      }

      op: "update",
      spec: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-maven_ref01"
          }
        }
      ]
    }

    {
      input: {
        ref: "maven_ref01",
        srcdatavar: "maven_ref01_data",
        suffix: "_dt0"
      }

      match: {
        id: "maven01",
        identifier: "identifier01",
        owner: "owner01"
      }

      op: "load",
      valid: [
        {
          apply: "TextFieldMark",
          def: {
            mark: "Mark01-maven_ref01"
          }
        }
      ]
    }
  ],
  "key$": "BasicMavenFlow"
}