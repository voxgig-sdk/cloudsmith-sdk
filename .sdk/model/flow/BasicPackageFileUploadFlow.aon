# BasicPackageFileUploadFlow

main: kit: flow: BasicPackageFileUploadFlow:
{
  entity: "package_file_upload",
  kind: "basic",
  name: "BasicPackageFileUploadFlow",
  step: [
    {
      input: {
        ref: "package_file_upload_ref01"
      }

      match: {
        identifier: "identifier01",
        owner: "owner01",
        repo: "repo01"
      }

      op: "create"
    }
  ],
  "key$": "BasicPackageFileUploadFlow"
}